import React from 'react';
import image from '../../images/giphy.gif'
import './purched.css'

const Perched = () => {
    return (
        <div className="add" >
            <img src={image} alt="" />
        </div>
    );
};

export default Perched;